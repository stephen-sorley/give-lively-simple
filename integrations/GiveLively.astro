---
/**
 * Astro adapter for code from the Give Lively Simple project:
 * https://github.com/stephen-sorley/give-lively-simple
 * 
 * To use this adapter, copy the following files from Give Lively Simple into
 * your project, putting them all in the same directory.
 *   GiveLively.astro (this file)
 *   gl-simple-builder.ts
 *   gl-simple-runtime.ts
 *   gl-simple.css
 * 
 * See gl-simple-builder.ts for a description of the options that you can pass
 * into this component.
 * 
 * NOTE: do not include this component more than once on the same page, or it
 *       will fail with non-unique ID's.
 */

import { createHTML, type CreateHTMLOptions } from "./gl-simple-builder";

import "./gl-simple.css";

// If Astro's CSP support is enabled, add the required CSP directives for Give Lively,
// but only on pages where the donation widget is actually present.
Astro.csp?.insertDirective("frame-src https://secure.givelively.org");

// Have this component accept the same options as createHTML from gl-simple-builder,
// and pass those options along without modification.
export interface Props extends CreateHTMLOptions {};

const donationWidgetHTML = createHTML(Astro.props);

---

<Fragment set:html={donationWidgetHTML} />

<script src="./gl-simple-runtime.ts" ></script>

<!--
  To override the default CSS styles, uncomment the style tag below, then
  modify it as you see fit. You may also add this style tag elsewhere on the
  page, if you prefer.
-->
<!--
<style is:global>
.gl-simple-donation-widget {
  /* ---------------------------- */
  /* Common settings to override. */
  --gl-font-family: Avenir, Montserrat, Corbel, 'URW Gothic', source-sans-pro, sans-serif; /* Geometric Humanist - modernfontstacks.com */
  --gl-font-size: 1rem;

  --gl-color-fg: light-dark(#4A4A4A, #E2E8F0);     /* foreground color */
  --gl-color-bg: light-dark(#FFF, #164046);        /* background color */
  --gl-color-accent: light-dark(#0CA1B3, #00CBBC); /* accent color - used for borders and shadows */
  --gl-color-focus: light-dark(#F2853A, #F9C727); /* color of ring applied when keyboard is focused */
  --gl-color-error: light-dark(#9f0712, #ff6467); /* color used for form error messages */

  /* --------------------------------------------------------------------- */
  /* Uncommon/advanced settings that you probably don't need to mess with. */
  --gl-color-shadow: color-mix(in oklch, var(--gl-color-accent), rgba(0,0,0,0.05) 85%);
  --gl-color-checked: color-mix(in oklch, var(--gl-color-accent), transparent 95%);

  --gl-radius: 0.5rem; /* border radius, used for all borders */

  --gl-spacing-2xs: 0.25em;
  --gl-spacing-xs: 0.5em;
  --gl-spacing-sm: 1em;
  --gl-spacing-md: 1.5em;

  --gl-duration: 120ms;
  --gl-duration-long: calc(2.5 * var(--gl-duration)); /* used for dialog open/close transitions */

  --gl-widget-border: 2px solid var(--gl-color-accent);
  --gl-widget-shadow: /* applied to whole widget, and to dialog windows */
    0 4px 6px -1px var(--gl-color-shadow),
    0 2px 4px -2px var(--gl-color-shadow);
  --gl-button-shadow: 0 4px 4px -2px var(--gl-color-shadow);
}
</style>
-->